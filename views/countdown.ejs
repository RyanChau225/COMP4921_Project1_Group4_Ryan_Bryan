<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Redirecting...</title>
    <!-- Links -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="stylesheet" type="text/css" href="/css/styles.css">
    <!-- End of Links -->
</head>

<body>
    <!-- Sticky Header -->
    <section class="hero is-fullheight is-default is-bold">
        <div class="hero-head">
            <!-- Include your header content here -->
            <%- include("templates/header") %>
        </div>

        <div class="hero-body">
            <!-- Centered Redirecting Content -->
            <div class="container has-text-centered">
                <div class="columns is-vcentered is-centered">
                    <div class="column is-8-desktop">
                        <div class="box">
                            <p class="title is-3">
                                You will be redirected to the 
                                <strong><%= shortURL %></strong> 
                                or 
                                <strong><%= customURL %></strong> 
                                in <span id="seconds"><%= seconds %></span> seconds...
                            </p>
                        </div>
                    </div>
                </div>
            </div>
            <!-- End of Centered Redirecting Content -->
        </div>

        <div class="hero-foot">
            <!-- Include your footer content here -->
            <%- include("templates/footer") %>
        </div>
    </section>

    <input type="hidden" id="seconds-data" data-seconds="<%= seconds %>">
    <input type="hidden" id="url-data" data-url="<%= url %>">
    <script src="/js/countdown.js" type="text/javascript"></script>
</body>

</html>
